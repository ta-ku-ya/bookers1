<h1>New book</h1>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit '投稿' %>
<% end %>



<h1>投稿一覧</h1>

<% @books.each do |book| %>

  <P>タイトル</P>
  <span><%= book.title %></span>

<% end %>
